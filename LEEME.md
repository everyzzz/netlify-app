## Crear app (en la carpeta que queramos)
npx create-react-app netlify-app 

## Instalar funciones netlify  
npm install @netlify/functions

## Iniciar tsc config
npx tsc --init

## Instalar ts de forma global
npm install -g typescript

## Iniciar SV
netlify dev